# Production Python FastAPI Backend URL (e.g. deployed on Railway, Render, Fly.io, AWS)
# In local development, leave this commented out or empty to use the Vite /api proxy.
# VITE_API_URL=https://insightpulse-api.up.railway.app
